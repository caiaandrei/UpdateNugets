<Window x:Class="UpdateNugets.UI.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UpdateNugets.UI.View"
        mc:Ignorable="d"
        Title="Update NuGets" 
        WindowState="Maximized"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <DockPanel Margin="5" Grid.ColumnSpan="2">
            <Button Content="Chnage The Path" 
                    DockPanel.Dock="Right"
                    Margin="2" 
                    Command="{Binding ChangePathCommand}"
                    CommandParameter="{Binding}"/>

            <TextBox Text="{Binding ProjectPath}" 
                     Margin="2"
                     IsReadOnly="True"/>

        </DockPanel>
        
        <local:NuGetsListView x:Name="NuGetsListView" 
                              Grid.Row="1"
                              Grid.RowSpan="2"
                              Margin="5"
                              DataContext="{Binding NuGetsListViewModel}"/>

        <local:SelectedNuGetDetailsView Grid.Column="1"
                                        Grid.Row="1"
                                        Margin="5"
                                        DataContext="{Binding SelectedNuGetDetailsViewModel}"/>

        <local:SelectedNuGetVersionFilesView Grid.Column="1"
                                             Grid.Row="2"
                                             Margin="5"
                                             DataContext="{Binding SelectedNuGetVersionFilesViewModel}"/>
    </Grid>
</Window>
